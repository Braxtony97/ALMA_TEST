public static class Enums
{
    public enum SceneType
    {
        Load,
        MainMenu,
        Gameplay
    }

    public enum ScreenType
    {
        LoadScreen,
        MainMenuScreen,
        GameplayScreen
    }
}
